spring:
  thymeleaf:
          cache: false
          mode: HTML5
          encoding: UTF-8
          prefix: classpath:/templates/
          suffix: .html
          servlet:
            content-type: text/html
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/wky
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
server:
  port: 8080
